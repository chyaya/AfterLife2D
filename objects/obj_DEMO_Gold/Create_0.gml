// This is an example instance for a potion object that can be spawned.
// Note that it is NOT child of the BSP collision parent, so other 
// instances can be spawned on top of it.

// Scale for the demo rooms... 
image_xscale = (global.cellSize / 16);
image_yscale = image_xscale;
